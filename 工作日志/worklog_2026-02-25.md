# 2026-02-25 工作日志

## 完成事项
- 优化售后 Agent 工作流：从“文档检索生成”改为“文档锚点定位 + 知识库回答”
- 增强 API 文档锚点获取能力：新增 TOC 抽取并兼容 VuePress 标题结构
- 完成 Dify 工作流配置排障：变量绑定、迭代输出、结构化输出约束
- 排查 Dify 调用日志缺失与批次执行可视化问题，补充批次调用日志
- 清理过程数据（分析任务/结果/日志、审核记录、QA、原始消息、操作日志）

## 关键实现/调整
- 后端新增并优化 `/api/docs/toc`：返回标题锚点 + 描述（desc），用于 LLM 锚点匹配
- 针对 HTML 内容结构差异，增加 header-anchor 兜底解析
- 规范 HTTP 节点调用方式与参数（path），避免 422/空结果
- LLM 结构化输出纠错：强制 JSON 输出与变量上下文注入
- 分批调用流程加“调用开始/完成”日志

## 问题与处理
- Markdown 文档爬取不支持 → 用后端 API 抓取并解析
- HTML 返回无锚点 → 调整解析规则兼容 VuePress
- LLM 输出非结构化 → 增加 user 约束与 schema 校验
- 迭代输出不可命名 → 使用输出变量映射/中间变量解决

## 当前结果
- 锚点目录可正确获取（含标题与描述）
- 工作流已能输出“锚点链接 + 知识库回答”
- 分批流程日志可追踪


## 补充：Dify 工作流与知识库联调
- 清理 Dify 后台卡住任务：将 `workflow_runs` 与 `workflow_node_executions` 中 `running` 状态批量标记为 `failed`，并清空 `workflow_pauses`、`celery_taskmeta`/`celery_tasksetmeta` 与 Redis 队列。
- 修复 LLM3 未使用知识库内容的问题：确认知识检索输出为 `result`（数组），提示词绑定正确变量；必要时用 Jinja2 模板转换拼接为文本再传入 LLM3。
- 解决 API 文档锚点链路报错：定位 `structured_output.snippets` 缺失导致变量引用失败，补全结构化输出或调整变量引用路径。
- Agent 调用工作流联调完成：可根据用户问题自动判断是否检索 API 文档锚点，测试通过。
