# 工作日报 - 2026-02-26

## 今日完成
- 排查 Dify 工作流知识检索“变量过滤无效”问题，定位到后端未解析 `{{#node.var#}}` 变量与多路检索分支未使用解析结果。
- 修复 Dify 源码：
  - `KnowledgeRetrievalNode` 增加过滤条件解析并用于 multiple 分支。
  - `VariablePool` 变量正则支持 `-`（节点 ID 含横杠）。
  - 增加检索过滤解析日志用于验证。
- 调整 Dify Docker 使用本地源码构建：
  - `docker-compose.yaml` 改为 `context: ../api` + `api/Dockerfile`。
  - 重新 build 并验证容器内代码已更新。
- 更新意图分类索引 `docs/llms.txt` 为 JSON 数组输出规范。
- 实现“分段规则更新”：
  - `upload_to_dify.py`、`app/services/dify.py`：除“常见问题”外分段 `max_tokens=1000`，常见问题保持自动分段。
  - `update_dify_segmentation.py`：批量重建文档以应用新分段规则，并加入 `pre_processing_rules` + `separator`，过滤非正式文档目录。
- OpenAPI 文档补充“接口概要”，覆盖全部 148 个接口，补齐接口作用、关键参数与结果说明。
- 运行清理脚本并重建知识库分段，36 份文档全部重建成功。
- 知识库检索生成 MVP 闭环完成：企微群聊分析 + 知识库 + 历史闭环 QA 生成回复，知识库随最新数据自动动态更新维护，可接入钉钉/企微 bot 使用。

## 关键验证结果
- 工作流日志中出现 `knowledge_retrieval metadata filters resolved`，变量已成功解析为 `basic-problems`。
- Dify API 自定义分段规则必须包含 `pre_processing_rules`，否则 400。
- `update_dify_segmentation.py` 重建 36/36 成功。

## 遇到问题
- 仅重建 `api` 无法生效，因原 docker-compose 使用远端镜像；已改为本地构建。
- `update_dify_segmentation.py` 初版对工具文档也执行导致 400，已过滤目录并补全自定义分段字段。
- 迭代检索报错：`created_by` 为空导致 UUID 入库失败（待修复）。

## 待办 / 下一步
- 修复 `dataset_queries` 写入时 `created_by` 为空导致 UUID 报错（在 Dify 源码侧兜底或确保调用传 user）。
- 如果需要，移除临时检索日志打印。

## 明日计划
1. QA 闭环数据入库向量化（已标准化流程，获取数据后自动切分自动入库）。
2. 售后 agent 接入企微 bot 测试。
3. 输出合规限制及合规测试。
