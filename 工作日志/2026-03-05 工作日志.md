## 2026-03-05 工作日志

1. **Dify QA 知识库解析为 0**  
   **问题**：QA 知识库上传后文档显示 0 字符/无分段，所有新文档解析结果为空。  
   **处理**：定位到 worker 报错 `Failed to format qa document`，确认 QA 解析依赖 LLM；配置缺失导致解析失败。完成 LLM 配置后重新验证解析链路。  
   **结果**：确认根因是 LLM 未配置，解析链路恢复后可正常生成 QA 分段。

2. **OpenAPI 官方文档信息过于零散**  
   **问题**：`/Users/joey/ai_crm/docs/[API接口] openapi-ai.md` 结构松散，不利于知识库与检索。  
   **处理**：按固定模板重排（方法/URL/必填/请求参数/请求示例/响应示例/错误码），并进一步抽取“纯干货”版本，仅保留规则、表格与示例。  
   **结果**：生成结构化版本 `docs/[API接口] openapi-ai_模板整理版.md` 和纯干货版本 `docs/[API接口] openapi-ai_模板整理版_纯干货.md`，便于导入与检索。

## 待办 / 后续计划
- 对 QA 知识库现有文档执行重解析或重建，确保历史文档恢复正常分段。
- 若需要进一步压缩文档，提供“去示例/仅参数与规则”版本。

## 风险与注意事项
- QA 解析依赖 LLM 配置，配置缺失会导致文档解析为 0。
- 纯干货版本可能移除上下文说明，使用时需确认是否影响读者理解。

## 决策记录
- QA 解析异常的根因判定为 LLM 未配置，优先通过配置修复而非调整上传格式。
- OpenAPI 文档整理采用“模板化 + 纯干货”双版本输出以兼顾可读性与检索效率。
